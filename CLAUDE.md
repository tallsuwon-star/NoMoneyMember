# 개발 규칙

## 이 파일(CLAUDE.md) 관리 원칙

- 새 프로젝트를 시작할 때는 항상 이런 CLAUDE.md 파일을 먼저 만든다.
- 사용자가 이후 대화 중에 어떤 기능/선호를 언급했을 때, 그게 중요도가 높아
  보이거나(반복적으로 강조, 여러 화면에 공통 적용될 성격) 사용자의 업무
  스타일과 접점이 큰 내용이면, 바로 규칙으로 추가하지 말고 먼저
  "이거 CLAUDE.md에 올릴까요?"라고 물어본 뒤 동의하면 이 파일에 추가한다.

## 목록/로그에는 항상 검색(필터) 기능 넣기

체크박스 명단, 로그 출력, 표 등 항목 수가 많아질 수 있는 UI를 새로 만들 때는
처음부터 검색/필터 입력창을 같이 넣는다. Electron 창은 브라우저 기본 Ctrl+F가
안 먹히므로, 텍스트 입력 후 실시간으로 항목을 필터링하는 자체 검색 기능을
직접 구현해야 한다. (자매 프로젝트 `ts_summervacation`에서 정한 규칙을 그대로 따름.)

예시: `electron/renderer/js/views/delinquentReport.js`의 `log-search`(실행 로그 검색) 참고.

## 일일정산달력 조회 관련 메모

- 일일정산달력(`/edu/admin_common/assessment_cm/index.php`)의 '미납자기간설정' 검색은
  한 번에 최대 6개월까지만 조회 가능하다는 전제로 구현했다(사용자 확인 사항).
  그래서 2023-01-01부터 기준일 전날까지를 반기(1~6월/7~12월) 단위로 쪼개 각각
  조회한 뒤 합산한다 (`python/worker/utils/periods.py`).
- '검색' 버튼(`totalClassList()`)은 항상 `detail_mode=102`(원어민 미납)로 조회되므로
  별도 옵션 없이 그대로 쓰면 '미납자' 인원수를 얻을 수 있다.
- 결과 인원수는 결과 하단의 "Showing 1 page of N entries" 문구의 N을 우선 사용한다.
  이 값은 페이지가 여러 장으로 나뉘어도 항상 전체 인원수라서(페이지네이션 순회 불필요),
  이 문구를 못 찾을 때만 결과 표의 데이터 행(합계 행 제외) 개수로 대체한다
  (`python/worker/lms/delinquent.py`의 `_parse_result_count`).
